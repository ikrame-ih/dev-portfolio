# Local-only (not pushed to GitHub)
app/
references/

# Engineering notes in docs/ are tracked in git.

# Web app
web/node_modules/
web/dist/
web/.env
web/.env.*
!web/.env.example

# OS & editor
.DS_Store
Thumbs.db
*.local
.vscode/
.idea/

# Private Cursor Agent memory (never commit)
AGENTS.md
**/AGENTS.md

# Agent skill installs (local only, never commit)
.agents/
.claude/
skills-lock.json
