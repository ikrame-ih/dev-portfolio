# Local-only (not pushed to GitHub)
app/
references/

# Engineering notes in docs/ are tracked in git.

# Web app
web/node_modules/
web/dist/
web/.env
web/.env.*
!web/.env.example

# OS & editor
.DS_Store
Thumbs.db
*.local
.vscode/
.idea/

# Agent skill installs (local scratch — never commit)
.agents/

